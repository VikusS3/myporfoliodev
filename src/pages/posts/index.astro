---
import Layout from "@/layouts/Layout.astro";
import { getCollection } from "astro:content";

// Obtenemos y ordenamos los posts por fecha (más recientes primero)
let posts = await getCollection("posts");
posts = posts.sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
);
---

<Layout title="Blog" description="Artículos sobre desarrollo y tecnología">
  <section class="max-w-5xl mx-auto">
    <h1 class="text-4xl font-bold mb-8 text-center">📚 Últimos Posts</h1>

    <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
      {
        posts.map((post) => (
          <a
            href={`/posts/${post.slug}`}
            class="relative group flex flex-col gap-3 text-neutral-300/70 rounded-xl border border-neutral-700
          overflow-hidden cursor-pointer hover:text-neutral-200 transition-all duration-300"
          >
            <div class="p-6 flex flex-col h-full">
              <h2 class="text-xl font-semibold mb-2 ">{post.data.title}</h2>
              <p class="text-sm mb-4">{post.data.date}</p>
              <p class="flex-grow">
                {post.data.description || "Sin descripción..."}
              </p>
              <span class="mt-4 font-medium">Leer más →</span>
            </div>
          </a>
        ))
      }
    </div>
  </section>
</Layout>
